# Deny by default: ~/.claude links to this repo, so only the files listed below are published;
# everything else, new files included, stays out.
*
!.gitignore
!CLAUDE.md
!settings.json
!statusline-command.sh
!agents/
!agents/*.md
!hooks/
!hooks/*.sh
!rules/
!rules/*.md
!.claude/
!.claude/CLAUDE.md
